---
name: jerrybay-v4-pm
description: Run JERRYBAY v4 repository work as a gate-controlled original-first implementation.
---

# JERRYBAY v4 PM

1. Read `AGENTS.md`, `docs/jerrybay-v4/00_GOAL.md`, `04_STATE.md`, the active work order, and `git status` before editing.
2. Reconcile the current Gate with the exact HEAD and select the smallest reversible slice that closes the highest-value remaining gap.
3. Preserve the static architecture unless the acceptance contract makes a change necessary.
4. Restore before redesign; do not create alternate IA or visual concepts.
5. Run the evidence commands required by the active work order and record only actual results.
6. On PASS, update `04_STATE.md` and stop at the next approval boundary. On FAIL, make only contract-linked remediation. On a true Owner blocker, stop without substituting an assumption.
