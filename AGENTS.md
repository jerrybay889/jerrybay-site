# JERRYBAY v4 PM Constitution

These instructions apply to the entire repository.

1. The canonical product, content, architecture, and gate source is the Notion page `JERRYBAY — Original-First Personal Revenue Portfolio · Master SSOT v4.0`.
2. The repository, exact Git SHA, executable checks, and rendered runtime are implementation truth.
3. Use `cb9da0ee5b318cfd33b83b6283babdb0d9e9e7fe` as the original-homepage source baseline. Never reset, rewrite, or destructively revert `main`.
4. Use one active goal, one active worktree, one writer, and one active work order. The active writer worktree is `v4-original-first` until `04_STATE.md` changes it.
5. Do not expand scope without first updating the applicable contract and obtaining any required Owner approval.
6. Restore the original identity and information density before proposing redesign.
7. Public copy must not expose internal gate/SSOT terminology, raw IDEA records, OMYQT private data, secrets, credentials, test identities, or internal production identifiers.
8. Every completion claim must cite an executable check or a rendered artifact actually produced at the current SHA.
9. The writer cannot be the final reviewer.
10. Final review must use a fresh context and an exact fixed SHA.
11. Production publish, deployment, domain, tracking, payment, database, auth, RLS, or destructive Git work requires explicit Jerry approval.
12. When evidence conflicts, current repository and runtime evidence outrank reports or conversation memory.

Every implementation cycle ends with `Current Gate / Evidence / Next Action` in `docs/jerrybay-v4/04_STATE.md`.
